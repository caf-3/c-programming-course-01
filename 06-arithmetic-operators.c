#include <stdio.h>

int main() {

    // arithmetic operators

    // + (addition)
    // - (addition)
    // * (addition)
    // / (addition)
    // % (addition)
    // ++ (addition)
    // -- (addition)

    int x = 5;
    int y = 2;

    // float z = x / (float) y;

    // printf("%.2f", z);
    int z = x % y;

    printf("%d", z);

    return 0;
}